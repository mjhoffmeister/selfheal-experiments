# Copilot cloud agent instructions — `selfheal-experiments`

<!--
INTENTIONALLY LEFT NEAR-EMPTY.

Per the experimental protocol (see /experiments/README.md), the first batch of
trials runs with NO per-repo guidance. This file exists so the agent does not
have to guess that the repo lacks one. Do NOT add prescriptive content here
until at least one trial batch has been run unguided and the result recorded.

Filled in step X6 of the bootstrap plan.
-->

This repository is an experiment harness; per-repo agent guidance is intentionally
withheld for the initial trial batch.
